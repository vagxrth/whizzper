import React from 'react'

type Props = {
    children: React.ReactNode,
    type: 'FREE' | 'PRO'
}

const Subscription = ({ children, type }: Props) => {
  return (
    children
  )
}

export default Subscription